/**
 * This is show case how coverage is being merged
 * @param input
 */
export const checkCov = (input: string) => {
  if(input === 'cypress'){
    if(input === 'cypress'){
      return 'cypress';
    }
    
    return 'nononreac';
  } else {
    return 'jest';
  }
}